# Shell scripts run inside Linux-based Docker builds — a CRLF shebang line (introduced by
# core.autocrlf=true on Windows checkouts) breaks them with "bash\r: No such file or directory".
*.sh text eol=lf
